import {FC, ReactNode} from 'react';

// TODO: server-side only, add children to context or something for rendering the server
const Head: FC<{children: ReactNode}> = ({children}) => null;

export default Head;
